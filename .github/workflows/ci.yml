name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: '3.12'

      - name: Install ESPHome
        run: pip install esphome

      - name: Create test secrets
        run: |
          cat > secrets.yaml << EOF
          wifi_ssid: "test_ssid"
          wifi_password: "test_password"
          EOF

      - name: Check tests/all-keys.yaml is current
        run: python tests/generate.py --check

      - name: Validate configuration
        run: esphome config example.yaml

      - name: Validate every generated entity
        run: esphome config tests/all-keys.yaml

      - name: Validate the manual-only shape
        run: esphome config tests/manual-only.yaml

  build:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: '3.12'

      - name: Install ESPHome
        run: pip install esphome

      - name: Create test secrets
        run: |
          cat > secrets.yaml << EOF
          wifi_ssid: "test_ssid"
          wifi_password: "test_password"
          EOF

      - name: Compile firmware
        run: esphome compile example.yaml

      - name: Compile every generated entity
        run: esphome compile tests/all-keys.yaml

      - name: Compile the manual-only shape
        run: esphome compile tests/manual-only.yaml
